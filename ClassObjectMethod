// 1. Creating a class with name Car
class Car {
    //  Declaring two attributes (instance variables) for the class
    private String color;  // This attribute stores the color of the car
    private String name;   // This attribute stores the name/brand of the car

    // 3. Constructor method that initializes the attributes
    // It takes two parameters: color and name
    public Car(String color, String name) {
        this.color = color;  // Initialize the color attribute with the passed parameter
        this.name = name;    // Initialize the name attribute with the passed parameter

        //  Printing a message inside the constructor
        // This message will be printed when the object is created
        System.out.println(color + " is a car color");
    }

    // 4. A method called bark that prints the car's brand name
    public void bark() {
        // Printing a message with the car's name/brand
        System.out.println(name + " is a brand name");
    }
}

//  The main class to run the program
public class Main {
    public static void main(String[] args) {
        // 2. Creating a new Car object named car1
        // We pass "Blue" as the color and "Audi" as the name
        Car car1 = new Car("Blue", "Audi");

        // Calling the bark() method on the car1 object
        // This will print the message with the car's name
        car1.bark();
    }
}
